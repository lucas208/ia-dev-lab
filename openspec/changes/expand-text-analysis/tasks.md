## 1. Palindrome Verification

- [ ] 1.1 Update the palindrome verifier to skip non-letter characters and compare letters case-insensitively; verify the specified phrase, mixed-case, non-palindrome, null, and letterless cases produce the required boolean results.
- [ ] 1.2 Add automated Java tests for the palindrome-verification scenarios and verify they compile and pass with the project's JDK, without adding external dependencies.

## 2. Text Analysis

- [ ] 2.1 Create a small text-analysis API that returns word, character, and vowel counts according to the text-analysis specification; verify it handles punctuation, mixed case, repeated whitespace, empty strings, whitespace-only input, and null input.
- [ ] 2.2 Add automated Java tests for every text-analysis scenario and verify they compile and pass with the project's JDK, without adding external dependencies.

## 3. Integration Verification

- [ ] 3.1 Compile all changed source and test files from the command line and verify no generated class files are retained in the repository.
- [ ] 3.2 Update the README with concise usage examples for both capabilities and verify its commands match the implemented class names.
